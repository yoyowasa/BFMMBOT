# .env.example：この雛形を .env にコピーして使う（.env はコミット禁止）
# 役割：bitFlyer 認証とリージョン指定（文書「手順10」準拠）
BF_API_KEY=__PUT_YOUR_KEY__        # ここにAPIキーを入れる
BF_API_SECRET=__PUT_YOUR_SECRET__  # ここにAPIシークレットを入れる
BF_REGION=jp                       # リージョンは jp 固定（CFD/FX_BTC_JPY 前提）
